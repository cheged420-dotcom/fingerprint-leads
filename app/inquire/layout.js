export const metadata = {
  title: "Get a Quote — Fingerprint Builders",
  description: "Tell us about your construction project and we'll be in touch.",
};

export default function InquireLayout({ children }) {
  return children;
}
